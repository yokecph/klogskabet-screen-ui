// Some constants used in the gallery

const width = screen.width;
const height = screen.height;

module.exports = {
  screenWidth: width,
  screenHeight: height,
  listWidth: Math.floor(width * 0.35), // 35% of screen width per column
  listMargin: 30,
  listCount: 4
};
